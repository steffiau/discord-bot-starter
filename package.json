{
  "name": "discord-bot-starter",
  "version": "0.1.0",
  "description": "Starter kit for a simple discord chat bot",
  "main": "index.js",
  "scripts": {
    "watch": "npx tsc --watch",
    "compile": "npx tsc",
    "start": "npm run compile && node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^15.12.2",
    "eslint": "^7.28.0",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "discord.js": "^12.5.3",
    "dotenv": "^10.0.0"
  }
}
